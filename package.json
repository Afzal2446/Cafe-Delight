{
  "name": "cafe-qr-order",
  "version": "1.0.0",
  "description": "QR Code based cafe ordering system",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npx ng build",
    "dev": "node server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "sql.js": "^1.10.2",
    "qrcode": "^1.5.3"
  }
}
